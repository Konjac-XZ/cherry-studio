Place paired Markdown spacing fixtures in this directory.

Each case uses matching file names:

- `<case>.input.md`
- `<case>.output.md`

The fixture test runner auto-discovers these pairs and compares the exact text,
including original line endings and trailing newlines.
